import React from 'react';
import {Card, Col, Table} from "react-bootstrap";


const DisciplineItem = ({disciplines}) => {
    console.log(disciplines)
    return (
        <>
        </>
    );
};

export default DisciplineItem;